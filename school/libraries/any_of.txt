Итератор на начало (begin), итератор на символ, следующий за последним (last), компаратор.
Проверяет, удовлетворяют ли компаратору  хотя бы один из элементов, находящихся с begin до last.
Нет
vector<int> a;
bool comp (int a) {
	return a<0;
};
int main () {
	for (int i=0; i<10; i++) vector.push_back(i);
	cout << all_of(a.begin(), a.end(), comp); //Выведет 0, т.е. false
	a[0]=-10;
	cout << all_of(a.begin(), a.end(), comp); //Выведет 1, т.е. true
	return 0;
};
